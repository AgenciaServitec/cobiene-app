import { FacebookProvider, Page } from "react-facebook";

export const IframeFb = () => {
  return (
    <section
      style={{
        width: "100%",
        display: "flex",
        justifyContent: "center",
        marginTop: "2em",
      }}
    ></section>
  );
};
